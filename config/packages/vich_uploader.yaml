vich_uploader:
    db_driver: orm

    mappings:
        images:
            uri_prefix: '%app.path.images%'
            upload_destination: '%kernel.project_dir%/public%app.path.images%'
        cv_doc:
            uri_prefix: '%app.path.cv_doc%'
            upload_destination: '%kernel.project_dir%/public%app.path.cv_doc%'
